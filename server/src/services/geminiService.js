import {GoogleGenerativeAI} from '@google/generative-ai'; const empty={name:'',email:'',phone:'',dob:'',gender:'',address:{full:'',city:'',state:'',pincode:''},education:[],experience:[],skills:[]};function model(){if(!process.env.GEMINI_API_KEY)throw Object.assign(new Error('GEMINI_API_KEY is not configured on the server.'),{status:503});return new GoogleGenerativeAI(process.env.GEMINI_API_KEY).getGenerativeModel({model:'gemini-2.0-flash',generationConfig:{responseMimeType:'application/json'}})}export async function extractProfileFromText(text,type){const prompt=`Extract profile data from this ${type} OCR text. Return ONLY a JSON object following this schema: ${JSON.stringify(empty)}. Use empty strings, empty arrays, or null where unknown. Never invent values. OCR text:\n${text.slice(0,30000)}`;const response=await model().generateContent(prompt);try{return {...empty,...JSON.parse(response.response.text())}}catch{throw new Error('AI response was not valid profile JSON.')}}export async function chatAboutProfile(message){const response=await model().generateContent(`Help the user with their application profile. Be concise. User: ${message}`);return response.response.text()}
