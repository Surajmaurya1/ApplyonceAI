import {Router} from 'express'; import multer from 'multer'; import {extractText} from '../controllers/ocr.controller.js';const router=Router();const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:10*1024*1024},fileFilter:(req,file,cb)=>/^(image\/(png|jpeg|webp)|application\/pdf)$/.test(file.mimetype)?cb(null,true):cb(new Error('Only images and PDFs are allowed'))});router.post('/extract',upload.single('document'),extractText);export default router;
