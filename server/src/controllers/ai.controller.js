import {extractProfileFromText,chatAboutProfile} from '../services/geminiService.js'; export async function extractProfile(req,res,next){try{const {ocrText,documentType='unknown'}=req.body;if(!ocrText?.trim())return res.status(400).json({success:false,error:{message:'OCR text is required'}});const profileData=await extractProfileFromText(ocrText,documentType);res.json({success:true,data:{profileData,fieldsExtracted:Object.values(profileData).filter(Boolean).length}})}catch(e){next(e)}} export async function chat(req,res,next){try{res.json({success:true,data:{message:await chatAboutProfile(req.body.message||'')}})}catch(e){next(e)}}
